/** Shared input styling for the rent-ledger forms. Matches the app's other
 *  dialog forms so the ledger doesn't look bolted on. */
export const RENT_INPUT_CLASS =
  "w-full border rounded-md px-3 py-2 text-sm bg-background";
