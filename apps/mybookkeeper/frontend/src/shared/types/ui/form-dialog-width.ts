/**
 * How wide a form dialog is allowed to grow.
 *
 * "narrow" suits a handful of fields read top to bottom; "wide" suits a form
 * long enough to be grouped into sections, where paired fields sit side by
 * side and a narrow column would strand half the row empty.
 */
export type FormDialogWidth = "narrow" | "wide";
