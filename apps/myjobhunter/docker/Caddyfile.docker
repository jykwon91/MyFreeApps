{$DOMAIN:localhost} {
    handle /api/* {
        uri strip_prefix /api
        reverse_proxy api:8002
    }

    handle {
        root * /srv/frontend
        try_files {path} /index.html
        file_server
    }
}
