{$DOMAIN:localhost} {
    handle /api/* {
        uri strip_prefix /api
        reverse_proxy api:8001
    }

    handle {
        root * /srv/frontend
        try_files {path} /index.html
        file_server
    }
}
