# Claude Code local settings (not shared)
.claude/settings.local.json
.claude/worktrees/
.claude/codebase-brief.md
.claude/scheduled_tasks.lock
.claude/e2e-passed

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Dependencies
node_modules/

# Build output
build/
dist/
.next/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local
*.env.docker
# Local Cloudflare R2 publish creds for the MGA ship-library tool (never commit).
.env.r2

# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.cursor/

# Database dumps (large binaries)
*.dump
*.sql
!**/alembic/**/*.sql
!packages/shared-backend/scripts/*.sql

# Playwright
**/playwright-report/
**/blob-report/
**/e2e/test-results/
**/e2e/.auth-token
**/e2e/.auth-org
**/e2e/.worker-pid
**/e2e/fixtures/documents/all-test-documents.zip

# Test documents
test-documents/*.pdf
test-documents/*.xlsx

# Output
output.txt
test-results/

# Lock files
.~lock.*
ROADMAP.md
MEMORY_AUDIT_*.md

# Dev-server stdout/stderr capture (per-app .uvicorn-*.log / .vite-*.log).
# Useful for ad-hoc inspection of a backgrounded server; never committed.
**/.uvicorn-*.log
**/.vite-*.log
